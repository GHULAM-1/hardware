/** Sentinel error messages thrown by server actions and mapped to friendly toasts in the UI. */

/** A supplier with the given phone already exists (phone is the supplier unique key). */
export const DUPLICATE_PHONE = "DUPLICATE_PHONE";

/** A staff member with the given CNIC already exists (CNIC is unique when present). */
export const DUPLICATE_CNIC = "DUPLICATE_CNIC";
